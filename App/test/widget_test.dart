// Smoke test basique — les vrais tests unitaires sont dans test/unit/
import 'package:flutter_test/flutter_test.dart';

void main() {
  test('les tests unitaires sont dans test/unit/', () {
    // Ce fichier est conservé pour compatibilité avec flutter test par défaut.
    // Voir test/unit/ pour les vrais tests.
    expect(true, isTrue);
  });
}
